// RunViewer strings — live SSE run output panel (header labels, empty state).
export default {
  liveOutput: 'Live output',
  toolActivity: 'Tool activity',
  noOutputYet: 'No output yet. Events stream here live once the run starts.',
}
